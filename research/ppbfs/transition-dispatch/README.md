# Transition-dispatch experiments

Reserved inside the shared PPBFS lab. No experiment has started. Any future work
must reuse `../common/` and graduate to its own clean `contrib/*` branch if it
becomes independently upstream-worthy.
