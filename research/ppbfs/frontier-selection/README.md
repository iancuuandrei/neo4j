# Frontier-selection experiments

Reserved inside the shared PPBFS lab. No experiment has started. Any future work
must reuse `../common/` and remain independent of the direct-state-index
contribution unless a measured architectural dependency is unavoidable.
